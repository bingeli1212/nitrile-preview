{
  "name": "nitrile-preview",
  "main": "./lib/nitrile-preview",
  "version": "0.0.0",
  "description": "A preview of LATEX document generated from Markdown.",
  "keywords": [],
  "activationCommands": {
    "atom-workspace": "nitrile-preview:toggle"
  },
  "deserializers": {
    "nitrile-preview/NitrilePreviewView": "deserializeNitrilePreviewView"
  },
  "configSchema": {
    "liveRefresh": {
      "type": "boolean",
      "default": false,
      "description": "Set it to true if you want the preview to be refreshed whenever source editor editor has been modified. Set to false if you only want the preview to be updated when the content is saved."
    },
    "defaultZoomLevel": {
      "type": "number",
      "default": 1.0,
      "minimum": 0.5,
      "maximum": 5.0,
      "description": "Set it to something greater than 1 to change the starting zoom factor."
    }
  },
  "repository": "https://github.com/bingeli1212/nitrile-preview",
  "license": "MIT",
  "engines": {
    "atom": ">=1.0.0 <2.0.0"
  },
  "dependencies": {
    "fs": "*",
    "jszip": "^3.2.1"
  }
}
